# バドミントン大会アプリ
## コート配置・審判管理UI全面改修 指示書

---

## 実装状況（2026-09-17時点）

Phase 1（必須項目、30章参照）は完了。Phase 2・Phase 3は未着手。以下、主な差分・補足。

### 完了
- コートカードの再設計、主審・副審／線審の常時テキスト表示、代役⚠️表示
- 「審判変更」ダイアログ（候補一覧・🟢🟡🔴の色分け・理由表示・「本部スタッフへ依頼」・「自動割り当てに戻す」）
- 手動変更した審判のロック・永続化（`matches.manual_referee`列をSupabaseに追加し、リロード後も維持）
- タップでの選択→コート配置（既存のドラッグ＆ドロップも維持）
- 「次の審判予定」相当の情報は、待機試合リストの各カードに「次審判予定(第◯C)」バッジとして表示（29章の独立パネル形式ではなく、この形に変更）
- 状態表示・操作ボタンの整理（ヘッダーに状態別アクション、審判情報ブロックに「審判変更」、さらに状態をひとつ前に戻す「戻る」ボタンを追加）

### 仕様変更（本指示書の記載と異なる形で決着）
- **コート解除ボタンは廃止**（2章・20章の前提と異なる）。ユーザーの明示判断により、単独の「コート解除」操作は削除し、代わりに「戻る」ボタンで状態を一段階ずつ巻き戻す方式に統一した（戻す過程でコートも解除される）。スコアシート印刷ボタンも同様の理由で廃止し、「試合受付」を押した時点で自動的に印刷ダイアログが開く方式に変更した。

### 未実装（Phase 2・3、今回スコープ外）
- 大会進行サマリーバー（全試合◯｜完了◯｜試合中◯…の常時表示）
- 「現在の審判一覧」サイドパネル
- コート変更UI（21章）
- 「次の1試合を自動配置」ボタン（17章）
- 複数試合一括自動配置・コート配置最適化提案・審判負担回数集計（Phase 3）

以下は元の指示書本文（作成時点の内容をそのまま残しています）。

## 1. 改修目的

大会当日の管理者が、コート配置・試合進行・審判割り当てを「画面を見ただけで判断できる」ようにUIを再設計する。

### 基本方針

- 現在実装されている試合進行・審判自動割り当てロジックは原則として維持する。
- 主な変更対象は管理者向けUI／操作導線とする。
- ドラッグ＆ドロップは残すが、必須操作にはしない。
- 「試合を選択 → 空きコートをタップ／クリック」の操作を基本操作とする。
- コート配置と審判情報を同じ画面で確認できるようにする。
- 自動割り当てされた審判を管理者が一目で確認できるようにする。
- 必要時のみ手動で審判を変更できるようにする。
- タブレット／PCの両方で操作しやすいUIとする。

---

# 2. 現在の実装を前提とすること

現在の `App.jsx` には、以下の機能が既に実装されているため、基本ロジックを再設計・破壊しないこと。

### コート関連

- コートごとの試合配置
- ドラッグ＆ドロップによる配置
- タップで試合を選択して空きコートへ配置
- コート解除
- コール
- 試合受付
- 試合中
- スコア入力
- 試合完了
- 完了済み試合のコート自動解除
- 試合番号表示
- スコアシート印刷

現在のコート管理画面では、空きコートへのドラッグ＆ドロップ、およびタップによる配置が既に実装されている。  
そのため、本改修では既存機能を削除せず、**タップ操作をより分かりやすい主操作に変更する**。

### 審判関連

現在の審判ロジックには以下がある。

#### 予選リーグ

- 直前試合の勝者を主審・副審候補にする
- 直前試合の敗者を線審候補にする
- 連戦等で担当できない場合は代役を探索
- 原則として同クラスから候補を探す
- 必要に応じて他クラスから応援
- 候補不足の場合は本部スタッフへ依頼

#### 決勝トーナメント

- 1回戦は予選リーグ敗退組を候補とする
- 2回戦以降は直前ラウンド敗退組を候補とする
- 決勝進出者は原則として審判を担当しない
- 候補不足の場合は予選リーグと同じフォールバックを利用
- 審判割り当ての重複防止
- 実際にコートへ配置された試合の審判はロックして維持
- 代役発生時の理由を `substitutionNotes` に保持

したがって、本改修では**審判計算ロジックを原則変更せず、表示・手動変更UIを追加／改善する**。

---

# 3. 新しい画面構成

管理者メニューの「コート進行・スコア」画面を、以下の構成へ変更する。

```text
┌──────────────────────────────────────────────────┐
│ 🏸 コート管理                                    │
│ 本日の試合：48　完了：12　進行中：8　待機：28     │
├──────────────────────────────────────────────────┤
│ 📢 次の審判予定                                  │
│ 第1コート　主・副審 A組　線審 B組                │
│ 第2コート　主・副審 C組　線審 D組                │
│ 第3コート　⚠️ 審判不足 → 本部スタッフ           │
├──────────────────────────────────────────────────┤
│ コート一覧                                      │
│                                                  │
│ [第1コート] [第2コート] [第3コート]             │
│ [第4コート] [第5コート] [第6コート]             │
│                                                  │
├──────────────────────────────────────────────────┤
│ 📋 待機試合                                      │
│ ① 第16試合 1部A  ○○組 VS △△組   [配置]         │
│ ② 第17試合 2部B  □□組 VS ◇◇組   [配置]         │
└──────────────────────────────────────────────────┘
```

---

# 4. 画面上部に「大会進行サマリー」を追加

画面最上部に、大会全体の状態を簡潔に表示する。

### 表示項目

- 全試合数
- 完了試合数
- 試合中
- コール待ち
- 待機試合数
- 空きコート数

例：

```text
全試合 48　｜　完了 12　｜　試合中 8　｜　
コール待ち 3　｜　待機 25　｜　空きコート 2
```

数字は `matches` と `config.courts` からリアルタイム算出する。

---

# 5. 「次の審判予定」セクションを追加

現在アプリ内部には、完了試合の勝者・敗者を次の審判候補として把握するロジックが存在する。

これを管理者向けUIに明示する。

## 表示例

```text
📢 次の審判予定

第1コート
主・副審　○○・○○
線審　　　△△・△△

第2コート
主・副審　□□・□□
線審　　　◇◇・◇◇

第3コート
⚠️ 審判不足
本部スタッフへ依頼
```

### 重要

「次審判予定」は情報表示であり、試合がまだコートへ割り当てられていない段階では、他の試合の配置を不必要にブロックしない。

現在の実装思想を維持する。

---

# 6. コートカードを再設計

現在のコートカードには試合・状態・審判情報・操作ボタンが存在する。

これを、管理者が必要な情報を最初から読める構成にする。

## 新しいコートカード

```text
┌──────────────────────────────┐
│ 第1コート              🟡 コール待ち │
├──────────────────────────────┤
│ 第15試合　1部　Aグループ             │
│                                      │
│ ○○太郎・○○次郎                      │
│              VS                      │
│ △△一郎・△△二郎                      │
│                                      │
├──────────────────────────────┤
│ 👤 主・副審　山田・佐藤               │
│ 🚩 線審　　　鈴木・高橋               │
│                                      │
│ [審判変更] [コート変更]              │
├──────────────────────────────┤
│ [コール] [試合受付] [スコア入力]      │
└──────────────────────────────┘
```

### 実装ルール

現在の「審判 ℹ️」バッジだけで詳細を表示する方式から、基本情報はカード上に常時表示する方式へ変更する。

ただし、スマホ・タブレットの画面幅が狭い場合は、詳細情報を折りたたむレスポンシブ表示としてよい。

---

# 7. 審判表示を改善

現在は「審判 ℹ️」ボタンを押すことで詳細情報を確認する。

これを以下へ変更する。

```text
主・副審　○○・○○
線審　　　△△・△△
```

さらに、代役が発生した場合は明確に表示する。

```text
⚠️ 代役あり
```

### 代役の表示例

```text
主・副審　○○・○○

⚠️ 本来：□□・□□
　連戦回避のため変更
```

または、

```text
線審　△△・△△

⚠️ 他クラスから応援
```

---

# 8. 「審判変更」機能を独立させる

コートカードに必ず

```text
[審判変更]
```

ボタンを配置する。

クリック／タップすると専用ダイアログを表示する。

---

# 9. 審判変更ダイアログ仕様

## タイトル

```text
第15試合　審判変更
```

## 現在の割り当て

```text
主・副審
○○・○○

線審
△△・△△
```

それぞれに、

```text
[変更]
```

ボタンを設置。

---

# 10. 審判候補一覧

審判候補は現在の審判ロジックと同じ制約を利用して一覧表示する。

## 表示例

```text
審判候補

🟢 ◎ 山田・佐藤
   同クラス・現在空き

🟢 ◎ 鈴木・高橋
   同クラス・現在空き

🟡 ○ 田中・伊藤
   他クラス

🔴 × 加藤・山本
   第3コートで試合中

🔴 × 渡辺・木村
   第5コートで審判中
```

### 候補の色分け

- 緑：推奨候補
- 黄：使用可能だが原則外
- 赤：現在割り当て不可

色だけに依存せず、必ず理由の文字も表示する。

---

# 11. 「本部スタッフへ依頼」を明示

候補が存在しない場合だけでなく、管理者が任意に選択できるようにする。

```text
[本部スタッフへ審判を依頼]
```

選択した場合は、

```text
主・副審　⚠️ 本部スタッフ
```

のように表示する。

---

# 12. 手動変更後の扱い

手動で変更した審判は、自動計算によって勝手に戻らないようにする。

### 必須仕様

- 手動変更した場合はその試合の審判をロック
- 試合終了後も履歴表示可能
- 次の試合の自動審判計算には、手動設定された担当者の使用状況を正しく反映
- 「自動割り当てに戻す」機能を用意する

例：

```text
審判変更済み

[現在の設定を維持]
[自動割り当てに戻す]
```

---

# 13. コート配置操作を「タップ中心」に変更

現在のドラッグ＆ドロップ機能は残す。

ただし、UI上は次の操作を主要導線とする。

## 操作

### Step 1

待機試合をタップ。

```text
第16試合を選択中
```

### Step 2

配置可能コートを強調。

```text
第1コート　🟢 配置可能
第2コート　🔴 試合中
第3コート　🟢 配置可能
```

### Step 3

空きコートをタップ。

### Step 4

即時配置。

---

# 14. 配置中の表示

試合を選択した状態では画面上部に、

```text
📌 第16試合を選択中

配置先の空きコートをタップしてください。

[キャンセル]
```

を表示。

現在実装されている `tapMoveSelection` の仕組みを利用・拡張する。

---

# 15. 空きコートの見た目

試合選択中は、配置可能なコートを明確にする。

```text
┌─────────────────┐
│ 第1コート       │
│                 │
│ ＋ ここに配置   │
└─────────────────┘
```

配置不可の場合：

```text
┌─────────────────┐
│ 第2コート       │
│ 🔴 試合中       │
└─────────────────┘
```

---

# 16. 待機試合一覧を改善

現在の「優先対戦リスト」は維持する。

ただし、「何番目に配置するべきか」が分かりやすい表示にする。

## 表示例

```text
📋 次に配置する試合

1　第16試合
   1部　Aグループ
   ○○・○○ VS △△・△△
   [配置]

2　第17試合
   1部　Bグループ
   □□・□□ VS ◇◇・◇◇
   [配置]
```

---

# 17. 「自動配置」機能を追加

新しいボタン：

```text
⚡ 次の試合を自動配置
```

または

```text
⚡ 空きコートへ自動配置
```

を追加。

### 自動配置ルール

既存の優先順位ロジックを利用して、

- 進行中の選手を除外
- 審判担当中の選手を除外
- 可能な限り連戦を避ける
- 優先対戦リスト上位から配置
- 空きコートへ順番に配置

する。

---

# 18. 「全部自動配置」ではなく段階的にする

事故防止のため、最初は

```text
[次の1試合を自動配置]
```

を推奨する。

必要に応じて将来的に、

```text
[空きコートへ最大○試合自動配置]
```

を追加できる構造にする。

---

# 19. コートカードの状態表示を統一

現在の状態：

- 要コール
- コール
- 試合受付
- 試合中
- 試合済

を、管理者が直感的に分かる短い表示へ整理する。

推奨：

```text
⚪ 空き
🟡 コール待ち
🔵 受付済
🔷 試合中
🟢 完了
```

ただし、既存の内部ステータス値は変更しない。

---

# 20. 操作ボタンの整理

コートカード下部のボタンは状態によって必要なものだけ表示する。

### コール待ち

```text
[コール]
[スコアシート]
[審判変更]
```

### 受付済

```text
[試合開始]
[審判変更]
```

※既存の「試合受付」処理名称を必要に応じて維持する。

### 試合中

```text
[スコア入力]
```

### 完了

```text
[スコア修正]
[詳細]
```

完了済み試合のコート解除ルールは現在仕様を維持する。

---

# 21. コート変更

コートカードに、

```text
[コート変更]
```

を追加してもよい。

ただし、試合中・受付済の試合を安易に移動できないようにする。

### 推奨

- waiting：変更可能
- calling：変更可能
- recepted：警告確認付き
- in_progress：原則変更不可
- completed：直接解除不可

---

# 22. 審判の視認性を上げる

大会本部では選手名よりも審判情報を確認したい場面があるため、

```text
👤 主・副審
🚩 線審
```

という固定ラベルを使用する。

「審判」だけでまとめず、役割を明示する。

---

# 23. 「審判一覧」表示を追加してもよい

コート一覧の上または横に、

```text
📋 現在の審判一覧

A組　→ 第1コート 主・副審
B組　→ 第1コート 線審
C組　→ 第2コート 主・副審
D組　→ 第2コート 線審
E組　→ 空き
```

という一覧を表示する。

これにより、管理者が

> 「今、審判になっている組は誰か？」

を一瞬で確認できる。

PCではサイドパネル、タブレットでは折りたたみエリアでもよい。

---

# 24. 管理者向けUIの基本原則

以下を優先順位として実装する。

1. 今、何をすればよいか分かる
2. 次にどの試合を配置するか分かる
3. どの組が審判なのか分かる
4. 審判変更が簡単
5. 誤操作しにくい
6. スマホ／タブレットでも操作できる
7. ドラッグ操作を必須にしない
8. 詳細情報を必要以上に常時表示しない

---

# 25. UIデザイン

現在の配色・デザインテイストを大きく崩さず改善する。

### 推奨カラー

- 緑：正常・推奨
- 黄：要注意・コール待ち
- 青：受付／進行
- 赤：エラー／配置不可
- 灰：未使用／無効
- 紫またはインディゴ：選択中

既存のTailwind CSSクラスを活用する。

---

# 26. レスポンシブ対応

## PC

- コートを3～4列
- 待機試合一覧を画面下または右側
- 審判予定を常時表示

## タブレット

- コート2列
- 待機試合は下部
- 審判情報はカード内表示

## スマホ

- コート1列
- 選択→配置を基本操作
- 審判詳細は折りたたみ可能
- ドラッグ＆ドロップを必須としない

---

# 27. 既存ロジックを壊さないための注意

以下のロジック・関数・状態を、理由なく削除・改変しない。

- `getRefereeForMatch`
- `getAllOccupiedRefereeIds`
- `getTournamentEliminatedPool`
- `getPredictedRefereeDetails`
- `getBusyTeamDetails`
- `getBusyTeamIds`
- `lockedReferees`
- `lastCourtReferees`
- `displacedCourtMatch`
- `tapMoveSelection`
- `getSortedWaitingMatches`
- コート割当処理
- 試合状態遷移処理

既存の審判自動割り当てロジックは、このUI改善の基礎として利用すること。

---

# 28. データ構造

UI改善のために新しい状態が必要な場合は、既存データとの整合性を優先する。

必要に応じて以下のUI専用stateを追加してよい。

```javascript
const [refereeEditModal, setRefereeEditModal] = useState(null);
const [selectedRefereeRole, setSelectedRefereeRole] = useState(null);
const [courtViewMode, setCourtViewMode] = useState('board');
const [showRefereeOverview, setShowRefereeOverview] = useState(true);
const [selectedWaitingMatchId, setSelectedWaitingMatchId] = useState(null);
```

ただし、既存の状態と重複するstateを作らないこと。

---

# 29. 操作フロー

大会当日の基本操作を以下にする。

```text
待機試合を確認
       ↓
次の試合を選択
       ↓
空きコートを選択
       ↓
試合を配置
       ↓
審判自動割り当て
       ↓
審判を画面上で確認
       ↓
必要なら「審判変更」
       ↓
コール
       ↓
試合受付
       ↓
試合開始
       ↓
スコア入力
       ↓
試合完了
       ↓
次の試合・次の審判を自動更新
```

---

# 30. 完了条件

以下をすべて満たしたら本改修完了とする。

### コート配置

- [ ] 試合をタップして選択できる
- [ ] 配置可能コートが明示される
- [ ] 空きコートのタップで配置できる
- [ ] ドラッグ＆ドロップも引き続き利用できる
- [ ] 配置中は「配置先を選択してください」と明示される
- [ ] キャンセルできる

### 審判

- [ ] コートカードに主・副審が表示される
- [ ] コートカードに線審が表示される
- [ ] 代役がある場合に⚠️表示される
- [ ] 代役理由を確認できる
- [ ] 審判変更ダイアログがある
- [ ] 候補者と使用不可理由を確認できる
- [ ] 本部スタッフを選択できる
- [ ] 手動変更が自動計算で上書きされない
- [ ] 自動割り当てへ戻せる

### 進行

- [ ] 大会全体の進行状況が分かる
- [ ] 次の審判予定が分かる
- [ ] 現在審判中の組が分かる
- [ ] 待機試合の優先順位が分かる
- [ ] 次の試合を自動配置できる

### 操作性

- [ ] PCで操作しやすい
- [ ] タブレットで操作しやすい
- [ ] スマホで操作しやすい
- [ ] ドラッグ必須ではない
- [ ] 誤操作時に簡単にキャンセルできる

---

# 31. 実装時の優先順位

### Phase 1：必須

1. コートカード再設計
2. 審判情報の常時表示
3. 審判変更ダイアログ
4. タップ→コート配置を主要導線化
5. 次の審判予定表示

### Phase 2：推奨

6. 現在の審判一覧
7. 大会進行サマリー
8. 「次の1試合を自動配置」
9. コート変更UI

### Phase 3：将来拡張

10. 複数試合一括自動配置
11. コート配置の最適化提案
12. 審判負担回数の集計

---

# 32. Claude Codeへの実装指示

この改修では、既存機能を作り直すのではなく、

**「現在の試合・審判ロジックを維持しながら、管理者が大会当日に迷わず操作できるUIへ再設計する」**

ことを最優先とする。

特に、

> 試合を選ぶ  
> → 空きコートを押す  
> → 審判を確認  
> → 必要なら変更  
> → コールする

という一連の操作を最短距離にすること。

UIを変更する際には、既存の試合番号・状態遷移・スコア・審判自動割り当て・決勝トーナメント・バックアップ等の既存機能を壊していないことを確認する。

最後に、既存機能の回帰テストとして、

- 予選リーグ
- 決勝トーナメント
- 審判代役
- 他クラス応援
- 本部スタッフ
- 審判ロック
- コート変更
- 試合完了
- リロード後の状態復元

を確認すること。
