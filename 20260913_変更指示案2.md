# バドミントン大会アプリ 管理画面UI/UX改善指示書

---

## 実装状況（2026-09-17時点）

- **1. 審判の直接表示と「手動変更」機能**：完了。コートカードに主審・副審／線審を常時テキスト表示し、「審判変更」ボタンから候補一覧を見ながら手動変更できる（`matches.manual_referee`列で永続化、リロード後も維持）。
- **2. 待機試合リストからの「ワンクリック配置」ボタン**：未実装。ドラッグ＆ドロップ、またはタップでの選択→空きコートタップという2ステップ操作のまま。
- **3. コート状況の点滅表示／コート解除ボタンの独立スタイル化**：コート解除ボタンは独立スタイル化ではなく**廃止**し、状態を一段階ずつ戻す「戻る」ボタンに置き換えた（仕様変更）。試合済コートの点滅演出は未実装。
- **4. 審判・連戦によるブロック理由の明示**：完了。待機試合リストの各カードに「試合(第◯C)」「審判(第◯C)」の理由バッジを表示している。

## 概要
現状の `App.jsx` における「コート進行・スコア」画面の操作性を向上させ、管理者がより直感的かつスピーディに大会を進行できるようにするための改善案です。

## 改善項目

### 1. 審判の直接表示と「手動変更」機能の追加
* **現状**: コートカード内の「審判 ℹ️」「審判 ⚠️」バッジをホバーまたはタップしないと担当者が確認できない。
* **改善案**: 
  * コートカード内に主審・線審の名前を常時テキスト表示する。
  * 自動割り当てされた審判（特に「本部スタッフへ審判を依頼」となった場合など）を、管理者がクリックして任意のペアやスタッフ名に手動で書き換えられる機能を追加する（`lockedReferees` のステートを直接編集するモーダルを実装）。

### 2. 待機試合リストからの「ワンクリック配置」ボタン
* **現状**: 待機試合リストから空きコートへの配置は、ドラッグ＆ドロップまたはタップ選択が必要。
* **改善案**: 
  * 待機試合（優先対戦リスト）の各カードに、「空きコートへ自動配置」するワンクリックボタン、または「第◯コート」を選択できるプルダウンを追加する。
  * これにより、画面スクロールを伴うドラッグ操作の手間を省き、配置を迅速化する。

### 3. コート状況のカラーリングとステータスの強調
* **現状**: ステータスに応じて背景色が変わる仕様だが、次のアクションが直感的に分かりにくい場合がある。
* **改善案**: 
  * 「試合済（スコア入力済）」になったコートは、「次の試合が配置可能である（空きコートとして利用可能）」ことを管理者に強くアピールするため、緑色の枠線や背景を点滅させるなどの目立つUIに変更する。
  * コートから試合を外す「コート解除」ボタンを、誤操作防止と視認性向上のためにアイコン付きの独立したボタンスタイルに変更する。

### 4. 審判・連戦による「ブロック理由」の明示
* **現状**: 待機中の試合リストで、試合中・審判中のペアを含む試合はグレーアウトされる。
* **改善案**: 
  * グレーアウトするだけでなく、カード上に「なぜ配置できないのか」の理由（例：「第3コートで試合中」「第1コートで審判予定」）を赤字や警告アイコンとともに大きく明示する。
  * これにより、管理者が進行のボトルネックを瞬時に把握できるようにする。

## 実装上の注意点
* 上記の改修は主に `App.jsx` の `adminTab === 'matches'` のレンダリングロジックに対して行う。
* `lockedReferees` の更新ロジックを拡張し、手動上書きフラグを持たせるなどの状態管理の調整が必要となる。
